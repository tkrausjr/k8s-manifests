apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-priv-deploy
spec:
  selector:
    matchLabels:
      app: nginx-priv
  replicas: 2 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: nginx-priv
    spec:
      containers:
      - name: nginx-priv
        image: harbor.tpmlab.vmware.com/library/nginx:stable-alpine
        # image: nginx:stable-alpine
        ports:
        - containerPort: 80
        securityContext:
          privileged: true
